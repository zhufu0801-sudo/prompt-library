# 精选模板更新验证

2026-09-09：TypeScript 检查、生产构建、8 项单元测试及 HTTP API 集成检查通过。

- 首页专用接口只返回编程与视觉制作两个模板；完整历史库保留 311 条。
- 三种语言字段标识与选项顺序一致；用户自由文字和锁定字段保留。
- 元提示词可关闭；字面变量内容不会递归替换。
- 图片/动画使用相同模板的制作类型字段，三语均提供两种选择。
- 中文、英文、日文方案创建与重新读取、锁定状态保存通过。
- 原有收藏、方案更新、访客隔离、跨域拒绝与禁用 AI 接口检查通过。
- 本地 SQLite 完整性、外键及索引检查通过。
- 已通过非浏览器 HTTP 200 预览检查；未进行浏览器交互或视觉测试。

语言切换仅翻译网站预设文案，用户输入与手工编辑的输出不自动翻译。旧模板保持原文。
此版本仍未接入模型，也未迁移到国内托管。
